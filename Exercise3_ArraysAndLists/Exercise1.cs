using System;
using System.Collections.Generic;
using System.Text;

namespace Exercise3_ArraysAndLists
{
    class Exercise1
    {
        //    var input = new List<string>();

        //        while (true)
        //        {

        //            Console.WriteLine("Add names : ");
        //            var names = Console.ReadLine();

        //            if (String.IsNullOrWhiteSpace(names))
        //                break;

        //            input.Add(names);
        //        }

        //        if(input.Count > 2)
        //            Console.WriteLine("{0}, {1} and {2} other people liked your post",input[0],input[1],input.Count-2);    
        //        if(input.Count == 2)
        //            Console.WriteLine("{0} and {1} liked your post",input[0],input[1]);
        //        if (input.Count == 1)
        //            Console.WriteLine("{0} liked your post",input[0]); 
    }
}
